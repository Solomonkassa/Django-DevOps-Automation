# nginx/ssl/ssl-config.conf
# SSL configuration template

# SSL session cache
ssl_session_cache shared:SSL:50m;
ssl_session_timeout 1d;
ssl_session_tickets off;

# SSL protocols (disable old protocols)
ssl_protocols TLSv1.2 TLSv1.3;

# SSL ciphers (modern configuration)
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

# Prefer server ciphers
ssl_prefer_server_ciphers on;

# DH parameters
ssl_dhparam /etc/nginx/ssl/dhparam.pem;

# HSTS (HTTP Strict Transport Security)
add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

# OCSP Stapling
ssl_stapling on;
ssl_stapling_verify on;
resolver 8.8.8.8 8.8.4.4 1.1.1.1 valid=300s;
resolver_timeout 5s;

# SSL buffer size
ssl_buffer_size 4k;

# SSL early data (0-RTT)
ssl_early_data on;

# SSL stapling file
ssl_stapling_file /etc/nginx/ssl/stapling/ocsp.staple;

# Certificate files
ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;

# Client certificate authentication (optional)
# ssl_client_certificate /etc/nginx/ssl/client-ca.pem;
# ssl_verify_client on;
# ssl_verify_depth 2;
